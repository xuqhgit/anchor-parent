<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.anchor.ms.auth.mapper.RoleMapper">

	<sql id="roleColumns">
        role.CODE as code,role.CREATE_TIME as createTime,role.CREATOR_ID as creatorId,role.ID as id,role.NAME as name,role.STATE as state,role.UPDATE_TIME as updateTime
	</sql>

	<!-- 修改 -->
	<update id="update" parameterType="role">
		update SYS_ROLE
		<set>
			
			<if test="code != null">
				CODE = #{ code },
			</if>
			<if test="createTime != null">
				CREATE_TIME = #{ createTime },
			</if>
			<if test="creatorId != null">
				CREATOR_ID = #{ creatorId },
			</if>
			<if test="id != null">
				ID = #{ id },
			</if>
			<if test="name != null">
				NAME = #{ name },
			</if>
			<if test="state != null">
				STATE = #{ state },
			</if>
			<if test="updateTime != null">
				UPDATE_TIME = #{ updateTime },
			</if>

		</set>
		where ID = #{id}
	</update>


	<insert id="insert" parameterType="role">
		INSERT INTO SYS_ROLE(
			CODE,CREATE_TIME,CREATOR_ID,ID,NAME,STATE,UPDATE_TIME
		)
		VALUES(
			#{ code },#{ createTime },#{ creatorId },#{ id },#{ name },#{ state },#{ updateTime }
		)
	</insert>


	<delete id="delete" parameterType="java.lang.Long">
			DELETE FROM SYS_ROLE where id = #{id}
	</delete>


	<!-- 根据主键获取 -->
	<select id="get" parameterType="java.lang.Long" resultType="role">
		SELECT
		<include refid="roleColumns" />
		FROM  SYS_ROLE role
		where id = #{ id }
	</select>

	<!-- 获取列表 -->
	<select id="getList"  resultType="role">
		select
		<include refid="roleColumns" />
		from SYS_ROLE role
	</select>

	<select id="getListByPage" parameterType="com.anchor.core.common.query.QueryPage" resultType="role">
		select
		<include refid="roleColumns" />
		from SYS_ROLE role
	</select>

</mapper>